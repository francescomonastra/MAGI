cff-version: 1.2.0
message: "If you use MAGI in work that leads to a publication, please cite it."
title: "MAGI: Multivariate Autoencoder for particle Generative Inference"
abstract: >-
  MAGI is a Conditional Variational Autoencoder that learns the joint
  phase-space distribution of particles crossing a surface in a Monte Carlo
  simulation - energy, position, direction and particle type - and samples new,
  physically consistent events at a fraction of the cost of rerunning the full
  transport. It is intended as a drop-in particle source that lets a long
  simulation be cut at a surface and resumed from a cheap sampler, which pays
  off when the events reaching that surface are rare for physical or
  geometrical reasons. The v0.8 head models the energy spectrum as a gated
  mixture of a normalizing-flow continuum and fixed-position line components
  pinned at measured atomic transition energies, with a learned conditional
  prior that preserves energy-geometry correlations through generation.
type: software
authors:
  - given-names: Francesco
    family-names: Monastra
    email: francesco.monastra@inaf.it
    affiliation: "Istituto Nazionale di Astrofisica (INAF)"
    # orcid: "https://orcid.org/0000-0000-0000-0000"   # TODO: add before release
version: 0.8.2
repository-code: "https://github.com/francescomonastra/MAGI"
keywords:
  - Monte Carlo
  - Geant4
  - generative model
  - variational autoencoder
  - normalizing flows
  - particle transport
  - instrument background
  - X-ray astronomy

# TODO before tagging the release:
#   1. add `license:` once chosen (must match the LICENSE file)
#   2. add `date-released:` with the actual tag date
#   3. add the author ORCID
#   4. replace the preferred-citation stub below with the published paper,
#      or delete it if the release precedes publication - a stub with
#      placeholder values is worse than no preferred-citation at all, because
#      reference managers will happily emit it verbatim.
